diff -r c065405e16bf process/factory.py
--- a/process/factory.py	Fri Oct 11 10:24:34 2013 -0400
+++ b/process/factory.py	Tue Oct 22 23:34:45 2013 -0700
@@ -2032,6 +2032,10 @@
         if uploadMulti:
             return
 
+        talos_files = [WithProperties('%(packageUrl)s')]
+        if 'win32' in self.complete_platform:
+            talos_files.append(WithProperties('%(testsUrl)s'))
+
         for master, warn, retries in self.talosMasters:
             self.addStep(SendChangeStep(
                          name='sendchange_%s' % master,
@@ -2040,7 +2044,7 @@
                          retries=retries,
                          branch=talosBranch,
                          revision=WithProperties('%(got_revision)s'),
-                         files=[WithProperties('%(packageUrl)s')],
+                         files=talos_files,
                          user=WithProperties('%(who)s'),
                          comments=WithProperties('%(comments:-)s'),
                          sendchange_props=sendchange_props,
@@ -2530,6 +2534,10 @@
             sendchange_props['pgo_build'] = False
 
         if not uploadMulti:
+            talos_files = [WithProperties('%(packageUrl)s')]
+            if 'win32' in self.complete_platform:
+                talos_files.append(WithProperties('%(testsUrl)s'))
+
             for master, warn, retries in self.talosMasters:
                 self.addStep(SendChangeStep(
                              name='sendchange_%s' % master,
@@ -2538,7 +2546,7 @@
                              retries=retries,
                              branch=talosBranch,
                              revision=WithProperties("%(got_revision)s"),
-                             files=[WithProperties('%(packageUrl)s')],
+                             files=talos_files,
                              user="sendchange",
                              comments=WithProperties('%(comments:-)s'),
                              sendchange_props=sendchange_props,
@@ -2846,6 +2854,10 @@
             'buildid': WithProperties('%(buildid:-)s'),
             'builduid': WithProperties('%(builduid:-)s'),
         }
+        talos_files = [WithProperties('%(packageUrl)s')]
+        if 'win32' in self.complete_platform:
+            talos_files.append(WithProperties('%(testsUrl)s'))
+
         for master, warn, retries in self.talosMasters:
             self.addStep(SendChangeStep(
                          name='sendchange_%s' % master,
@@ -2854,7 +2866,7 @@
                          retries=retries,
                          branch=talosBranch,
                          revision=WithProperties("%(got_revision)s"),
-                         files=[WithProperties('%(packageUrl)s')],
+                         files=talos_files,
                          user="sendchange",
                          comments=WithProperties('%(comments:-)s'),
                          sendchange_props=sendchange_props,
